# Optional root-level reference file for deployment setup.
# Copy values into the actual app env files used by your platform.

NEXT_PUBLIC_API_URL=http://localhost:4000
PORT=4000
CLIENT_ORIGIN=http://localhost:3000
